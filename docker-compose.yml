version: "3.9"

services:

  backend:
    build: ./backend
    container_name: splitwise-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: splitwise-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
